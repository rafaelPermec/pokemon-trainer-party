interface effectEntries {
  effect: string,
  language?: object,
  short_effect: string,
}

interface itemStdout {
  itemName: string,
  quickEffect: string,
}

export {
  effectEntries,
  itemStdout,
};
