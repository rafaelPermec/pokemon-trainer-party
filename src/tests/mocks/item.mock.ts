import { itemStdout } from '../../interfaces/IItems';

const item: itemStdout = {
  itemName: 'Space mail',
  quickEffect: 'Lets a Trainer write a message and send it via Pokémon trade.',
};

export default item;
